# AgentFS Nexus 已知问题与技术债务

> 盘点日期：2026-09-24；状态补记：2026-09-25。本文区分已有复现/记录的问题与从代码结构看出的风险。风险项需要通过真实环境或进一步设计确认；不把推测写成已复现 Bug。后续交付任务见[最终落地执行计划](./plans/AgentFS最终落地执行计划.md)。

## 1. 历史已解决问题与发布回归

### ISSUE-001：重新分析确认后的文件标签未可靠替换

- **状态：用户于 2026-09-25 确认已解决；原严重程度：高。**
- **现象证据：**问题手册记录真实 MySQL/API 操作中，预览只剩用户确认的新标签、归档批次和明细显示 `SUCCEEDED`，但文件详情仍返回原标签加新标签。记录的复现文件为 `file_id=8`。
- **根因线索：**旧运行 JAR 早于源码；现场仍运行旧增量标签逻辑。仅改源码而未重建并重启，问题仍会出现。
- **已有修复：**当前工作区的归档持久化逻辑先删除该文件全部标签关联，再写入确认集合，flush 后重新读取核验；不一致会抛错并回滚。删除改为原生 SQL。问题手册称 H2 回归已通过，启动脚本增加源文件或 `pom.xml` 新于 JAR 时拒绝启动的检查。
- **发布回归：**普通预览、重新分析、失败重试、幂等恢复及“标签不一致不能标成功”继续使用真实 MySQL + API + 页面验证；这是质量门禁，不再把 ISSUE-001 列为待修复缺陷。历史复现与关闭记录见[归档的问题手册](./archive/legacy/Cofferer待解决问题手册.md)。

## 2. 已确认的遗留实现与架构债务

### 2.0 存量数据完整性与部署暴露

- **P0，旧 MySQL 引用缺失**：盘点时 MySQL 有 7 条当前文件元数据，其中 ID 7、8 对应的 MinIO 正文对象在恢复副本中不存在。用户已确认整批旧单用户数据不迁移；切换时清理须在清理摘要中单独列出这 2 条，不能记成新系统迁移失败或自动补造正文。
- **P1，旧归档台账无法由当前对象验证**：14 条归档明细的 source/target 对象均在当前 MinIO 对象清单中缺失，包含 SUCCEEDED/SUCCEEDED 3 条、SUCCEEDED/NOT_REQUESTED 9 条和 SUCCEEDED/CONFLICTED 2 条。清理前保存其状态摘要；不得把旧 SUCCEEDED 当作正文仍存在的证明。
- **P1，MinIO 上游生命周期**：MinIO 官方开源仓库已于 2026-04-25 归档为只读，本机旧二进制不适合作为新生产栈基线。桌面版移除 MinIO；Web/NAS 保留 MinIO 时须确定受支持版本、更新渠道、漏洞处置、替换预案和独立备份。旧单用户 bucket 只在切换前用于快照核验和获授权的整桶清理，新实例使用独立 bucket。[官方仓库](https://github.com/minio/minio)
- **P1，MySQL 暴露所有网卡**：盘点时 Windows MySQL 服务的 3306 监听 0.0.0.0 与 ::。在服务器/NAS 生产部署中必须限制到私有容器网络或受信网段；公网只开放 HTTPS 反向代理。
- **对象保留风险**：97 个 MinIO 对象中约 92 个未被当前文件、归档路径和补偿表引用，是未引用候选而不是已确认垃圾。旧业务数据虽获用户授权在切换时清理，但必须使用显式目标/数量/摘要清单和可校验备份，不得直接清理 MinIO 内部 data 目录。
- **M0 快照**：MySQL 20 表/266 行逻辑备份已在隔离 schema 逐表恢复核验；119 个 MinIO 数据文件已全量复制并校验，隔离副本读取 97 个对象正文并计算 SHA-256。H2/Redis 快照也已保存；Redis 本次未独立启动恢复。详细证据见 [M0 盘点记录](./plans/M0-存量数据盘点与保护记录.md)。

### 2.1 旧标签确认事件可绕开正式治理闭环

**代码状态（2026-09-26）：已移除旧旁路。** `TagConfirmedEventListener`、`TagConfirmedEvent` 与旧 `StorageArchiveService` 已删除；标签确认不再直接移动对象。旧 `PUT /api/files/{id}/category` 保留兼容路由，但返回 409（业务码），要求通过整理预览和归档台账确认；前端已移除该直接写入调用。现有治理预览与归档操作台账成为唯一正式分类/位置变更入口。

R22 的其余完整性要求仍待完成：撤销快照需覆盖所有正式字段、对象摘要及 revision，且恢复后重建检索索引；需要证明批次幂等、失败隔离和对象/数据库不一致时不标成功。

### 2.2 生产环境尚未建立身份认证边界

验收文档把系统定义为单用户私人模式；当前 `pom.xml` 未引入 Spring Security，Controller 也未形成登录/授权层。`/api/settings/**` 可管理模型端点和密钥、文件 API 可操作数据；`/api/test/hello` 会发起模型调用并返回模型回复。CORS 仅限制浏览器跨域来源，不等于 API 身份认证。

因此当前形态应按本机/可信私网单用户应用理解，不具备共享或公开网络多用户部署边界。若要部署到不可信网络，需要先补认证、授权和接口访问策略。

### 2.3 模型调用可能保存或输出用户内容

- **代码状态（2026-09-26）：已完成正文日志与异步错误首轮收敛。** 模型诊断默认关闭；显式启用时只记录状态、耗时、模型名和用量，不记录会话 ID、用户输入或 AI 回复。LangChain4j 请求/响应日志已关闭；模型连接测试、对话、上传/解析、预览/归档和全局异常路径不再输出原始回复、正文、对象路径或异常文本，任务错误状态使用允许列表或固定提示。每日清理会擦除旧 `model_call_log` 中的正文、会话标识及原始错误字段，保留 30 天内的非内容诊断数据。
- 用户级模型配置、敏感/未知内容发送前授权、API Key 与主密钥分层管理、管理员内容隔离，以及端点 SSRF/重定向限制仍待 R10–R14 完成；生产部署日志仍须按 R51 抽检。

这不是已确认的密钥泄露事件，但文件内容/用户问题可能包含敏感信息。需要明确日志脱敏、开关、保存期限和导出/删除策略，并在实际部署日志配置中检查请求/响应正文是否打印。

### 2.4 事务和对象存储一致性路径复杂

- MinIO 对象复制、SQL 更新、删除旧对象不是一个原子事务；主治理路径依赖台账和补偿任务恢复。
- 历史 `StorageArchiveService` 的 `@Lazy` 自注入与 `REQUIRES_NEW` AFTER_COMMIT 方案已随旧旁路移除；正式对象变更现走持久归档操作台账和补偿任务。
- 删除失败或归档后留下的旧对象仍需要统一的孤儿对象对账、隔离和清理策略；不能依赖日志或假定存在后台 GC。

### 2.5 数据库关联一致性依赖应用层

迁移脚本没有为主要领域关系创建外键；例如 `file_tag_mapping.file_id/tag_id`、预览项与批次、操作明细与批次/文件等由应用层维护。这样便于补偿/迁移，但删除顺序、孤儿记录和数据修复需要应用测试保证。后续如考虑增加外键，应先核对历史数据、级联删除语义和 MySQL/H2 一致性。

### 2.6 新旧包结构并存

新业务边界在 `file`、`tag`、`task`、`governance`、`inbox`；旧的顶层 `service`、`repository`、`controller`、`entity` 仍承载跨领域或历史类。`Code-Wiki.md` 明确建议增量迁移，避免一次性大搬迁。短期债务是职责边界不完全一致、需要逐类判断代码归属。

## 3. 部署与质量欠账

| 项目 | 状态 / 影响 | 后续方向 |
|---|---|---|
| MySQL 真实场景验收 | ISSUE-001 已由用户确认解决，但仍需纳入发布回归；H2 不覆盖 MySQL 删除、事务和 FULLTEXT 行为差异 | 增加 MySQL 集成测试或 Testcontainers，完成真实页面验收 |
| 集成/故障测试 | C19 未完成。已有多类单元和 H2 测试；尚需验证本地文件系统、MinIO、数据库、Redis 可选路径、模型故障及撤销/幂等闭环 | 按 R50–R52 场景补全，并分别验证桌面无 MinIO 依赖、Web/NAS MinIO 适配与 H2/MySQL Flyway 构建 |
| Docker 部署 | C18 未完成；仓库没有 `docker-compose.yml` / Compose 清单 | 完成持久化卷、健康检查、服务依赖和环境变量样例 |
| 运行脚本环境绑定 | `start-all.bat` 中 JDK、Redis、MinIO 可执行文件/数据目录使用本机绝对路径，依赖 Windows、MySQL 已预装和本机凭据 | 提供可移植配置或安装说明；Compose 完成后统一部署路径 |
| Node 运行时版本 | 文档只要求 Node 20+，仓库没有看到 `.nvmrc` / Volta 版本锁文件；包版本由 lockfile 约束 | 记录并固定支持的 Node 主/次版本，构建环境按 lockfile 安装 |
| 备份与恢复 | 本次 M0 验证 MySQL dump 可还原、MinIO 副本可读；产品仍需分别验证桌面 DB + 本地文件库、Web/NAS DB + MinIO bucket/持久卷及加密主密钥的一致恢复。Redis 索引/会话可按规则重建；`AGENTFS_SECRET_KEY` 丢失会使已保存模型密钥无法解密 | 按 R35/R42 完成加密备份包、密钥托管、清单校验和整套恢复演练 |
| 桌面与服务端存储适配 | 当前实现仍直接以 MinIO 为对象存储，收件箱只是导入扫描；尚无共用 FileStoragePort 和桌面本地文件系统适配器 | 完成受控 FileStoragePort、owner 隔离、两种适配器的崩溃恢复和备份；桌面发布包移除 MinIO 运行依赖，Web/NAS 保留受控 MinIO 适配与服务配置，再做 Electron 与服务端/NAS 发布 |

## 4. 明确范围限制（不是已复现 Bug）

- 当前运行代码仍缺少多人认证与 owner 隔离；最终范围已改为三种形态都支持多账号，但尚未实现。
- 第一版不承诺音频、视频、压缩包、电子表格、演示文稿分析，也没有页码级引用。
- Embedding、Hybrid Search 和收件箱扫描默认关闭，需要显式配置。
- 前端/后端与 Redis、MinIO、数据库的本地部署脚本仍有机器环境前置条件。

## 5. 盘点边界

本文件记录仓库已有问题手册中的 ISSUE-001、交接文档明确指出的遗留路径、M0 存量服务快照以及通过配置/迁移/代码可以直接证实的风险和工程欠账；这不表示已排尽所有运行时缺陷。发现新的可复现问题时应按旧问题手册模板补充复现、影响、证据和关闭条件。当前优先任务见[最终落地执行计划](./plans/AgentFS最终落地执行计划.md)。
